<% form_for(@retired_basic_salary) do |f| %>
  <fieldset>
    <legend>添加离退休人员基本工资</legend>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p class="bbar">
      <%= f.submit '保存' %>
      <input type="button" onclick="$page.showListView()" value="取消" />
    </p>
  </fieldset>
<% end %>