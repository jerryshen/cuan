<% form_for(@retired_college_benefit) do |f| %>
  <fieldset>
    <legend>添加离退休人员学院补贴</legend>
    <%= render :partial => 'form', :locals => {:f =>f}  %>
    <p class="bbar">
      <%= f.submit '保存' %>
      <input type="button" onclick="$page.showListView()" value="取消" />
    </p>
  </fieldset>
<% end %>
