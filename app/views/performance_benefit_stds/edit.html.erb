<% form_for(@performance_benefit_std) do |f| %>
  <fieldset>
    <legend>修改员工绩效津贴标准金额</legend>
    <%= render :partial => 'form', :locals => { :f => f }  %>
    <p class="bbar">
      <%= f.submit '更新' %>
      <input type="button" onclick="$page.showListView()" value="取消" />
    </p>
  </fieldset>
<% end %>