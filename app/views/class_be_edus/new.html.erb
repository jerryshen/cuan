<% form_for :class_be_edu, :url => { :action => "create"} do |f| %>
  <fieldset>
    <legend>添加课时津贴</legend>
    <%= render :partial => 'form', :locals => { :f => f }  %>
    <p class="bbar">
      <%= f.submit '保存' %>
      <input type="button" onclick="$page.showListView()" value="取消" />
    </p>
  </fieldset>
<% end %>