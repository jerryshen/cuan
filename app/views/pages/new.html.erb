<% form_for(@page) do |f| %>
  <fieldset>
  <legend>新建页面</legend>
  <%= render :partial => 'form', :locals => { :f => f }  %>
  <p class="bbar">
    <%= f.submit '保存' %>
    <input type="button" onclick="$page.showListView()" value="取消" />
  </p>
  </fieldset>
<% end %>
