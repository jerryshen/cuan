<% form_for(@retired_basic_salary_record) do |f| %>
  <fieldset>
    <legend>修改离退休人员基本工资记录</legend>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p class="bbar">
      <%= f.submit '更新' %>
      <input type="button" onclick="$page.showListView()" value="取消" />
    </p>
  </fieldset>
<% end %>